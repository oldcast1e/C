#include  <stdio.h>
#include<string.h>
int main(){

    char arr[100],tmp;
    gets(arr);
    int len = strlen(arr);
    for(int i=0;i<len;i++){
        printf("%s\n",arr+i);
        // printf("%s",)
    }
    //처음부터 5까지
    //두번째부터 5까지 + 첫번째부터 첫번째 까지
    //세번째 부터 5까지 + 첫번째에서 두번째까지
}